# auth-service Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** SPEC-5: tight-контракты auth, Gateway на `API_ROUTES`, тихие логи сервисов. Prisma уже сделана. Lib-контроллер уже на `AUTH_PATTERNS`, guards/decorators в Gateway уже по порядку — верификация, не переделка.

**Architecture:** Контракты → Gateway-маршруты → логи сервисов → верификация lib-контроллера → фронт-аудит. Репозиторий/сессии/баны/лимиты (Redis) уже реализованы и покрыты спеками — не трогать.

**Tech Stack:** NestJS + Prisma + Redis + JWT, zod/`createZodDto`, Jest + supertest (gateway `auth.http.spec.ts`-паттерн), Nx из корня.

**Spec:** `docs/superpowers/specs/2026-09-13-auth-design.md`

## Global Constraints

- Nx только из корня; `--testPathPatterns=`; `npm` эксклюзивно.
- Контракты first; маршруты только `API_ROUTES`; паттерны только из констант.
- Пароли длиннее 128 символов запрещены контрактом (argon/bcrypt время + DoS-вектор).
- Логи: presence-флаги + `eventType`; сырые id/email/provider в логах запрещены.
- Спеки в `__tests__/`; живых broker/DB/Redis нет.
- `nx sync` дважды после межпакетных импортов.
- Существующие спеки (`auth.service.spec`, `auth.http.spec`, repo/cache/admin спеки) — не ломать;
  новые кейсы — дописывать, не переписывать.

---

## File Structure

| Файл | Ответственность |
|---|---|
| `libs/common/src/schemas/auth/*.schema.ts` (modify) | max-лимиты (перечень в Task 1) |
| `libs/common/src/schemas/auth/__tests__/auth-limits.schema.spec.ts` (create) | Спеки лимитов |
| `libs/common/src/constants/routes.ts` (modify) | Недостающие auth-маршруты |
| `apps/backend/gateway/src/controllers/auth.controller.ts` (modify) | Маршруты из констант |
| `libs/backend/auth/src/services/auth.service.ts` (modify) | 3 интерполяции id/provider → presence-флаги |
| `libs/backend/auth/src/services/verification.service.ts`, `cleanup.service.ts`, `admin/admin-ban.service.ts` (audit+fix) | Скан интерполяций секретов |
| `libs/backend/auth/src/services/__tests__/auth.service.spec.ts` (modify) | no-secrets кейс |
| Фронт `features/auth` (audit) | Формы уже на общих схемах — верификация |

---

### Task 1: Контракты auth

**Files:**
- Modify: `register.schema.ts`, `login.schema.ts`, `credentials.schema.ts`, `oauth-login.schema.ts`,
  `reset-password.schema.ts` (+`forgotPasswordSchema` внутри), `resend-verification.schema.ts`,
  `session.schema.ts` (device/os/browser/ip/country/userAgent maxes), `refresh-token.schema.ts`
  (tokenHash — строка без max? хэш фикс. длины: оставить без max, как в notification? нет:
  tokenHash sha256-hex 64 — но схема общая с refresh flow; поставить `.length(64)`? Осторожно:
  проверить использования `refreshTokenSchema` перед ужесточением. Если используется только
  типизация — `.length(64)` ок; если парсит внешние токены — оставить. Решение при исполнении,
  зафиксировать в коммите.)
- Также: `verification-query.schema.ts`, `session-param.schema.ts` — прочитать и добавить maxes
  по месту (token/ids).
- Create: `libs/common/src/schemas/auth/__tests__/auth-limits.schema.spec.ts`

**Interfaces:**
- Consumes: ничего
- Produces: tight-схемы (Task 2 pipe через существующие DTO — DTO подхватят лимиты автоматически)

- [ ] **Step 1: Написать падающий спек**

```ts
import { loginSchema } from '../login.schema';
import { oauthLoginSchema } from '../oauth-login.schema';
import { forgotPasswordSchema, resetPasswordSchema } from '../reset-password.schema';
import { registerSchema } from '../register.schema';
import { resendVerificationSchema } from '../resend-verification.schema';

describe('auth contract limits', () => {
  it('caps registration fields', () => {
    expect(
      registerSchema.safeParse({ email: 'a@example.com', password: 'Aa1!aaaa', username: 'u'.repeat(33) }).success,
    ).toBe(false);
    expect(
      registerSchema.safeParse({ email: `${'a'.repeat(250)}@ex.com`, password: 'Aa1!aaaa', username: 'alice' }).success,
    ).toBe(false);
    expect(
      registerSchema.safeParse({ email: 'a@example.com', password: `Aa1!${'a'.repeat(125)}`, username: 'alice' }).success,
    ).toBe(false);
  });

  it('caps login fields', () => {
    expect(
      loginSchema.safeParse({ email: `${'a'.repeat(250)}@ex.com`, password: 'x' }).success,
    ).toBe(false);
    expect(loginSchema.safeParse({ email: 'a@example.com', password: 'p'.repeat(129) }).success).toBe(
      false,
    );
  });

  it('caps oauth fields', () => {
    expect(
      oauthLoginSchema.safeParse({ provider: 'p'.repeat(33), providerId: 'x', email: 'a@example.com', name: 'alice' }).success,
    ).toBe(false);
    expect(
      oauthLoginSchema.safeParse({ provider: 'github', providerId: 'x', email: 'a@example.com', name: 'a' }).success,
    ).toBe(false);
  });

  it('caps recovery emails', () => {
    expect(forgotPasswordSchema.safeParse({ email: `${'a'.repeat(250)}@ex.com` }).success).toBe(false);
    expect(resendVerificationSchema.safeParse({ email: `${'a'.repeat(250)}@ex.com` }).success).toBe(false);
  });

  it('caps the new password', () => {
    expect(
      resetPasswordSchema.safeParse({ token: 't', newPassword: `Aa1!${'a'.repeat(125)}` }).success,
    ).toBe(false);
  });
});
```

Пароль `Aa1!aaaa` удовлетворяет `PASSWORD_REGEX` (верхний+нижний+цифра+спец, min 8) — проверить
regex перед использованием; если не проходит — подобрать валидный по факту regex.

- [ ] **Step 2: Запустить, убедиться что падает**

Run: `npm exec nx -- test @org/common --testPathPatterns="schemas/auth/__tests__/auth-limits" --skip-nx-cache`
Expected: FAIL (лимитов нет).

- [ ] **Step 3: Минимальные правки схем**

- `registerSchema`: `email: z.email().max(254)`, `password: ... .max(128)` (после regex-чейна),
  `username: z.string().min(2).max(32)`.
- `loginSchema`: `email: z.email().max(254)`, `password: z.string().min(1).max(128)`.
- `credentialsSchema` + `createCredentialsSchema`: `email .max(254)`.
- `oauthLoginSchema`: `provider .max(32)`, `providerId .max(128)`, `email .max(254)`,
  `name .min(2).max(32)`.
- `forgotPasswordSchema`/`resendVerificationSchema`: `email .max(254)`.
- `resetPasswordSchema`: `token .max(512)` (токены сброса длинные, но не бесконечные),
  `newPassword` — regex + `.max(128)`.
- `session.schema.ts`: `device/os/browser .max(64)`, `ip .max(45)`, `country .max(2)`,
  `userAgent .max(512)`, `tokenHash` — по решению из заголовка таска.
- `verification-query.schema.ts`, `session-param.schema.ts`: maxes по месту.

- [ ] **Step 4: Прогнать common целиком + зависимые DTO-проекты**

Run: `npm exec nx -- test @org/common --skip-nx-cache` — PASS.
Run: `npm exec nx -- run @org/auth:test --skip-nx-cache` — PASS (DTO подхватили лимиты;
  существующие спеки с длинными фикстурами могут упасть — чинить фикстуры, не схемы).
Run: `npm exec nx -- run @org/gateway:test --skip-nx-cache` — PASS.

- [ ] **Step 5: Commit**

```bash
git add libs/common/src/schemas/auth/
git commit -m "feat(common): tight auth contracts"
```

---

### Task 2: Gateway — маршруты из `API_ROUTES`

**Files:**
- Modify: `libs/common/src/constants/routes.ts` (расширить `auth`)
- Modify: `apps/backend/gateway/src/controllers/auth.controller.ts`

**Interfaces:**
- Consumes: tight-DTO (автоматически после Task 1)
- Produces: ничего нового

- [ ] **Step 1: Недостающие маршруты**

```ts
auth: {
  root: 'auth',
  register: 'auth/register',
  login: 'auth/login',
  logout: 'auth/logout',
  refresh: 'auth/refresh',
  verifyEmail: 'auth/verify-email',
  resendVerification: 'auth/resend-verification',
  forgotPassword: 'auth/forgot-password',
  resetPassword: 'auth/reset-password',
  github: 'auth/github',
  githubCallback: 'auth/github/callback',
  google: 'auth/google',
  googleCallback: 'auth/google/callback',
  sessions: 'auth/sessions',
  sessionById: (sessionId: string) => `auth/sessions/${sessionId}`,
},
```

Сверить буквальные строки в gateway-контроллере (253, 366, 373, 396, 403, 424, 460, 501)
и скопировать 1-в-1.

- [ ] **Step 2: Замена декораторов**

`@Controller('auth')` → `@Controller(API_ROUTES.auth.root)`;
`@Post('register')` → `@Post(API_ROUTES.auth.register.split('auth/')[1])`? Нет —
по прецеденту notification/search: корень из константы, суффиксы методов — литералы
(`'register'`, `'verify-email'`, `'sessions'`, `'sessions/:id'`, `'github'`...).
Полные пути-строки в `@Post('auth/...')` запрещены; относительные суффиксы разрешены,
т.к. корень зафиксирован константой. OAuth-колбэки `'github/callback'` — суффиксы.
Импорт `API_ROUTES` в контроллер.

- [ ] **Step 3: Прогнать gateway целиком (http-спеки ловят смену путей)**

Run: `npm exec nx -- test @org/gateway --skip-nx-cache`
Expected: PASS (`auth.http.spec.ts` ходит реальными путями через supertest — смена декоратора
сломается здесь же, если суффикс неверный).

- [ ] **Step 4: Commit**

```bash
git add libs/common/src/constants/routes.ts apps/backend/gateway/src/controllers/auth.controller.ts
git commit -m "refactor(auth): gateway on API_ROUTES"
```

---

### Task 3: Тихие логи сервисов

**Files:**
- Modify: `libs/backend/auth/src/services/auth.service.ts` (3 интерполяции: 113, 173, 292)
- Audit: `verification.service.ts`, `cleanup.service.ts`, `admin/admin-ban.service.ts`
  (grep `` logger.*(` `` + `payload`/` традиционные строки; править только интерполяции
  идентификаторов/секретов)
- Modify: `libs/backend/auth/src/services/__tests__/auth.service.spec.ts` (дописать no-secrets кейс)

**Interfaces:**
- Consumes: ничего
- Produces: ничего нового

- [ ] **Step 1: Скан**

Run: `grep -rn 'logger\.\(log\|debug\|warn\|error\|verbose\)(` libs/backend/auth/src --include="*.ts" | grep -v __tests__ | grep -v "eventType" | head -20`
Каждая строка с `${id}/${email}/${token}/${provider}` — кандидат. `provider` (имя провайдера
`github|google`) — низкая чувствительность, но тоже увести во флаг/константу ради единообразия.

- [ ] **Step 2: Правки (образец)**

```ts
// было:
this.logger.warn(`Service: Role lookup failed. User ID not found: ${id}`);
// стало:
this.logger.warn({ eventType: 'role_lookup_not_found', hasUserId: !!id });
```

Аналогично для 173 (`session_establish_user_not_found`), 292 (OAuth provider →
`{ eventType: 'oauth_pipeline_started', provider: dto.provider }`? provider — enum из двух
значений, не секрет; оставить значением, но именованным полем, не интерполяцией).

- [ ] **Step 3: No-secrets спек**

Дописать в существующий `auth.service.spec.ts` (стиль файла — проверить моки конструктора
с 9 зависимостями; переиспользовать существующий `beforeEach`):

```ts
it('never writes raw identifiers to logs', async () => {
  // вызвать register其次 validateCredentials с секретами через существующие моки,
  // собрать logger.mock.calls, assert not.toContain secrets
});
```

Точные вызовы — по факту существующего спека (прочитать его beforeEach при исполнении).

- [ ] **Step 4: Прогнать auth целиком**

Run: `npm exec nx -- test @org/auth --skip-nx-cache`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add libs/backend/auth/src/
git commit -m "refactor(auth): secret-free service logs"
```

---

### Task 4: Lib-контроллер — верификация (без правок кода по умолчанию)

**Files:**
- Verify: `libs/backend/auth/src/controllers/auth.controller.ts` (все хендлеры уже на
  `AUTH_PATTERNS`; `RpcException` для RPC-ошибок — проверить, что сервисы бросают
  `{ message, status }`-совместимое)
- Test: существующий `controllers/__tests__/auth.controller.spec.ts` — прогнать как есть

- [ ] **Step 1: Проверить транспортное разделение**

Все `@MessagePattern` — запрос/ответ (register/login/refresh/sessions/ban/...); чистых
`@EventPattern` быть не должно в этом файле (побочки уходят событиями из gateway в
notification). Если найдётся EventPattern с ответом — зафиксировать пробелом, чинить
только если ломает контракт.

- [ ] **Step 2: Прогнать**

Run: `npm exec nx -- test @org/auth --testPathPatterns="controllers/__tests__/auth.controller.spec" --skip-nx-cache`
Expected: PASS без правок. Иначе — чинить и коммитить `test(auth): ...`.

---

### Task 5: Фронт-аудит (без новой UI)

**Files:**
- Audit: `libs/client/features/auth/src/**` (формы уже на `loginSchema`/`registerSchema`;
  `register-form.tsx` — проверить `.extend({ confirmPassword })` + неотправку лишнего)

- [ ] **Step 1: Проверить расширение и тело запроса**

`use-register.ts` шлёт `z.infer<typeof registerSchema>` (без `confirmPassword`); форма резолвит
расширенную схему. Если факт расходится — точечный фикс в этом таске.

- [ ] **Step 2: Commit — только при правках.** Иначе пропуск.

---

### Task 6: Финальная верификация

- [ ] **Step 1: Проекты целиком**

Run: `npm exec nx -- run @org/auth:test --skip-nx-cache` — PASS.
Run: `npm exec nx -- run @org/common:test --skip-nx-cache` — PASS.
Run: `npm exec nx -- run @org/gateway:test --skip-nx-cache` — PASS.

- [ ] **Step 2: Формат своих файлов** — `npx prettier --write` строго на изменённых,
  повтор Step 1, commit `style(auth): prettier formatting for touched files`.

- [ ] **Step 3: Affected** — `npm exec nx -- affected --target=test --base=origin/dev --skip-nx-cache` — PASS.

- [ ] **Step 4: Доложить** — коммиты, результаты, пробелы.
