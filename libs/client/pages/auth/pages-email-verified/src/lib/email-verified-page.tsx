import { EmailVerifiedScreen } from '@org/features-auth';

export function EmailVerifiedPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <EmailVerifiedScreen />
    </div>
  );
}
